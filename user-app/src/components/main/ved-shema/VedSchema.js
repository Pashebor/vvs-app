import React from 'react';
import LeftSideBar from '../sidebar/LeftSideBar';
import OverallSchema from './OverallSchema';

const VedSchema = () => {
  return(
      <section className="ved">
          <LeftSideBar/>
          <OverallSchema/>
      </section>
  )
};

export default VedSchema;